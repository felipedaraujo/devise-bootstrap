default: &default
  adapter: postgresql
  encoding: UTF-8
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: protocol_development
  username: <%= `whoami`.chomp %>

test:
  <<: *default
  database: protocol_test
  username: <%= `whoami`.chomp %>
